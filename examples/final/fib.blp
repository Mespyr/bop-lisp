; fib ;
(let terms (list))
(let length 40)

(let num 0)
(let num_ 1)

(while (! (= (len terms) length))  
    (do (println num)
        (let temp (+ num num_))
        (let num num_)
        (let num_ temp)
        (append num_ terms)))